#pragma once

enum input_mode {
  INPUT_MODE_NAME,
  INPUT_MODE_CODE,
  INPUT_MODE_HYBRID,
};

enum timing_mode {
  COUNTDOWN,
  COUNTUP,
};

enum timer {
  NO_TIMER = -1,
};

enum quiz_mode {
  QUIZ_ALL = -1,
};
